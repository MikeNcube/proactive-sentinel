{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "gunicorn --bind 0.0.0.0:8080 --workers 2 --threads 4 --timeout 120 --log-level debug wsgi:app",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 300
  }
}
