{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE"
  },
  "deploy": {
    "startCommand": "sh -c \"python -m flask db upgrade && python scripts/seed_data.py && gunicorn --bind 0.0.0.0:\\$PORT app:app\"",
    "restartPolicy": "ON_FAILURE"
  }
}
